<!DOCTYPE html>
<html lang="pt">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Valoriza Stock</title>
  <style>
    body {
      margin: 0;
      font-family: 'Times New Roman', serif;
      background-color: #cc5500; /* laranja-escuro */
    }

    header {
      background-color: #fff;
      text-align: center;
      padding: 20px;
      border-bottom: 2px solid #ccc;
    }

    header h1 {
      margin: 0;
      color: #cc5500;
    }

    header p {
      margin: 5px 0 0;
      font-style: italic;
    }

    .form-container {
      background-color: white;
      width: 400px;
      margin: 50px auto;
      padding: 30px;
      border-radius: 10px;
      box-shadow: 0 0 10px rgba(0,0,0,0.2);
    }

    .form-container h2 {
      text-align: center;
      color: #cc5500;
    }

    .input-group {
      margin: 20px 0;
      position: relative;
    }

    .input-group input {
      width: 100%;
      padding: 10px 10px 10px 35px;
      font-size: 16px;
      border: 1px solid #ccc;
      border-radius: 5px;
    }

    .input-group i {
      position: absolute;
      top: 10px;
      left: 10px;
      color: #cc5500;
    }

    .form-container button {
      width: 100%;
      background-color: #cc5500;
      color: white;
      padding: 10px;
      border: none;
      border-radius: 5px;
      font-size: 16px;
      cursor: pointer;
    }

    /* Tela principal */
    .main {
      padding: 20px;
    }

    .top-bar {
      display: flex;
      justify-content: space-between;
      align-items: center;
      background-color: white;
      padding: 10px 20px;
      border-bottom: 2px solid #ccc;
    }

    .top-bar h2 {
      color: #cc5500;
      margin: 0;
    }

    .top-bar .actions button, .top-bar select {
      margin-left: 10px;
      padding: 5px 10px;
      font-family: 'Times New Roman', serif;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 20px;
      background-color: white;
    }

    th, td {
      border: 1px solid #ccc;
      padding: 10px;
      text-align: center;
    }

    th {
      background-color: #f2f2f2;
    }
  </style>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
</head>
<body>

  <header>
    <h1>Controlo de Stock</h1>
    <p>Organize o seu armazém com inteligência</p>
  </header>

  <div class="form-container">
    <h2>Registo da Empresa</h2>
    <div class="input-group">
      <i class="fas fa-building"></i>
      <input type="text" placeholder="Nome da Empresa">
    </div>
    <div class="input-group">
      <i class="fas fa-envelope"></i>
      <input type="email" placeholder="Email">
    </div>
    <div class="input-group">
      <i class="fas fa-phone"></i>
      <input type="tel" placeholder="Número de Telefone">
    </div>
    <button>Registar</button>
  </div>

  <!-- Tela principal -->
  <div class="main">
    <div class="top-bar">
      <h2>Ficha de Armazém</h2>
      <div class="actions">
        <button title="Adicionar Compra"><i class="fas fa-plus"></i> Compra</button>
        <button title="Adicionar Venda"><i class="fas fa-minus"></i> Venda</button>
        <button title="Devolução"><i class="fas fa-undo"></i> Devolução</button>
        <select>
          <option value="fifo">FIFO</option>
          <option value="lifo">LIFO</option>
          <option value="cmp">CMP</option>
        </select>
        <button>Gerar PDF</button>
        <button>Exportar Dados</button>
        <button style="background-color:red; color:white;">Logout</button>
      </div>
    </div>

    <table>
      <thead>
        <tr>
          <th>Data</th>
          <th>Descrição</th>
          <th>Entrada (Qtd * Preço = Valor)</th>
          <th>Saída (Qtd * Preço = Valor)</th>
          <th>Existências (Qtd * Preço = Valor)</th>
        </tr>
      </thead>
      <tbody>
        <!-- Linhas dinâmicas inseridas via JS -->
      </tbody>
    </table>
  </div>

</body>
</html>
